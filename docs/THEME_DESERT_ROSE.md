# Desert Rose — Nova Portal application notes

Applied from theme-factory theme #7 across the entire app via CSS custom properties in `src/app/globals.css`.

| Token | Hex | Role |
|---|---|---|
| Dusty Rose | `#d4a5a5` | Soft primary highlight |
| Clay | `#b87d6d` | Accent / CTAs / hazard stripe |
| Sand | `#e8d5c4` | Page background |
| Sand deep | `#ddc7b3` | Alternate surfaces |
| Deep Burgundy | `#5d2e46` | Ink / borders / primary buttons |

Fonts: Source Sans 3 (maps to FreeSans Bold / FreeSans).

Constraints preserved:
- Industrial hard edges (no border-radius)
- Mobile 48px minimum touch targets
- i18n still via `t(...)` en/fr/es
